[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/sh /app/docker/scripts/railway-entrypoint.sh"
healthcheckPath = "/"
healthcheckTimeout = 2100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
